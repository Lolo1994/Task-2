@model IEnumerable<Task_2.Models.BudgetAllocation>

@{
    ViewData["Title"] = "View";
}

<h1>View</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.disaterType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.disasterBudget)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.totalGoodsPurchase)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.budgetRemaining)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.disaterType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.disasterBudget)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.totalGoodsPurchase)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.budgetRemaining)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
