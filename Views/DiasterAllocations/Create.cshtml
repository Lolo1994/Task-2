@model Task_2.Models.DiasterAllocation

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>DiasterAllocation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="disasterType" class="control-label"></label>
                <select asp-for="disasterType" asp-items="@(new SelectList(ViewBag.disastertypes,"Id","disasterType"))"></select>
                <span asp-validation-for="disasterType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="dateAlloted" class="control-label"></label>
                <input asp-for="dateAlloted" class="form-control" />
                <span asp-validation-for="dateAlloted" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="amountAllotted" class="control-label"></label>
                <input asp-for="amountAllotted" class="form-control" />
                <span asp-validation-for="amountAllotted" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
